# Optional: enables Discord avatar lookup for DM participants.
# Create a Bot token in the Discord Developer Portal. Never use a user token.
DISCORD_BOT_TOKEN=

# Optional: enables Instagram public avatar lookup with your browser cookies.
# This is a logged-in IG session cookie. Users must accept the account risk.
IG_COOKIE=
